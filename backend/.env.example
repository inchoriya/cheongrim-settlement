# 로컬에서 토스 지급대행을 쓸 때만 복사: cp .env.example .env
# Spring이 .env를 자동 로드하지는 않음 → IDE Run Config / export 로 주입

PAYOUT_PROVIDER=mock
TOSS_SECRET_KEY=
TOSS_SECURITY_KEY=
TOSS_BASE_URL=https://api.tosspayments.com
TOSS_SCHEDULE_TYPE=SCHEDULED
# TOSS_PAYOUT_DATE=2026-08-10
